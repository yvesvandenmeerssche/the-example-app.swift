"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var __1 = require("..");
describe('rich-text-from-markdown', function () {
    it('should render some markdown', function () {
        var document = __1.richTextFromMarkdown('# Hello World');
        var text = document.content[0].content[0];
        expect(text.value).toBe('Hello World');
    });
    it('should call the fallback function when a node is not supported', function () {
        var fakeNode = { nodeType: 'image' };
        var fallback = jest.fn(function (node) { return fakeNode; });
        var document = __1.richTextFromMarkdown('![image](https://image.example.com/image.jpg)', fallback);
        expect(document.content[0].content[0].nodeType).toBe('image');
        expect(fallback).toBeCalledTimes(1);
        expect(fallback.mock.calls).toMatchSnapshot();
    });
});
//# sourceMappingURL=index.test.js.map