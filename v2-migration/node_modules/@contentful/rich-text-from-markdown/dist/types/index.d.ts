import { Document, Block } from '@contentful/rich-text-types';
export interface MarkdownNode {
    depth: string;
    type: string;
    ordered: Boolean;
    children: Array<MarkdownNode>;
    content: Array<MarkdownNode>;
    value: string;
}
export interface MarkdownTree {
    children: MarkdownNode[];
}
export declare function richTextFromMarkdown(md: string, fallback?: (mdNode: MarkdownNode) => Block): Document;
